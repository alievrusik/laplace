# Skill: Write Prototype Card

Use this after a project finishes.

Create a concise Markdown memory card with summary, domain, task type, inputs, output, approach, reuse notes, links, and limitations.
