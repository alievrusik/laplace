# Skill: Configure Foundation Model

Use this when a generated prototype needs model access.

Default deployed demos use Anthropic API. Local-only demos may use the vLLM endpoint. Never expose provider API keys to browser code.
