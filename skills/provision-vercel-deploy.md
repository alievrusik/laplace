# Skill: Provision Deploy

Use this when a project should get a preview URL.

The orchestrator owns deploy operations. The builder prepares the project for deployment but should not receive broad deployment credentials.
