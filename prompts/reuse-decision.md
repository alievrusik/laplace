# Reuse Decision Prompt

Compare a new prototype request against existing prototype cards.

Return up to three matches with:

- `slug`
- `title`
- `reason`
- `reuseNotes`

Only return matches that can realistically save implementation time.
